{
  "manifest_version": 2,
  "name": "Flag Bird",
  "description": "It may useful to capture the flag.",
  "version": "0.1",
  "app": {
    "background": {
      "scripts": ["background.js"]
    }
  },
  "permissions": [
    {"socket": ["tcp-connect:*:*"]}
  ]
}
